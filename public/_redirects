# Cloudflare Pages SPA redirect
# This redirects all non-file requests to index.html for React Router
# Cloudflare Pages automatically excludes existing files (index.html, assets/*) from redirects
/*    /index.html   200

